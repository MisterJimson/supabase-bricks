const myAuthRedirectUri =
    'com.{{organization_name}}.{{flutter_package_name}}://login-callback';

const supabaseUrl = '{{{supabase_url}}}';
const supabaseAnnonKey = '{{supabase_anon_public_key}}';
